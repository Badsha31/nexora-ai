import fs from 'node:fs'; import path from 'node:path'; import {DatabaseSync} from 'node:sqlite';
const file=process.env.NEXORA_DB||'./database/nexora.sqlite'; fs.mkdirSync(path.dirname(path.resolve(file)),{recursive:true});
export const db=new DatabaseSync(file); db.exec('PRAGMA journal_mode=WAL; PRAGMA foreign_keys=ON;');
export function migrate(){db.exec(`CREATE TABLE IF NOT EXISTS users(id TEXT PRIMARY KEY,username TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,role TEXT NOT NULL DEFAULT 'admin',created_at TEXT NOT NULL);CREATE TABLE IF NOT EXISTS sessions(id TEXT PRIMARY KEY,user_id TEXT NOT NULL,expires_at INTEGER NOT NULL,created_at TEXT NOT NULL,FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS policies(key TEXT PRIMARY KEY,enabled INTEGER NOT NULL,updated_at TEXT NOT NULL);CREATE TABLE IF NOT EXISTS projects(id TEXT PRIMARY KEY,name TEXT NOT NULL,root TEXT NOT NULL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);CREATE TABLE IF NOT EXISTS memories(id INTEGER PRIMARY KEY AUTOINCREMENT,project_id TEXT NOT NULL,type TEXT NOT NULL,key TEXT NOT NULL,value TEXT NOT NULL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(project_id,type,key),FOREIGN KEY(project_id) REFERENCES projects(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS repo_files(id INTEGER PRIMARY KEY AUTOINCREMENT,project_id TEXT NOT NULL,path TEXT NOT NULL,size INTEGER,hash TEXT,language TEXT,content TEXT,updated_at TEXT NOT NULL,UNIQUE(project_id,path),FOREIGN KEY(project_id) REFERENCES projects(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS operations(id INTEGER PRIMARY KEY AUTOINCREMENT,project_id TEXT,kind TEXT NOT NULL,status TEXT NOT NULL,command TEXT,output TEXT,error TEXT,started_at TEXT NOT NULL,finished_at TEXT);CREATE TABLE IF NOT EXISTS credentials(id TEXT PRIMARY KEY,name TEXT UNIQUE NOT NULL,ciphertext TEXT NOT NULL,iv TEXT NOT NULL,tag TEXT NOT NULL,created_at TEXT NOT NULL);CREATE TABLE IF NOT EXISTS audit(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id TEXT,action TEXT NOT NULL,details TEXT,created_at TEXT NOT NULL);`); const defaults={deployment:true,terminal:true,file_write:true,model_access:true,android_build:true,network:false,github_push:true,export_zip:true}; for(const [k,v] of Object.entries(defaults)) db.prepare('INSERT OR IGNORE INTO policies VALUES(?,?,?)').run(k,v?1:0,new Date().toISOString());}
