let token=localStorage.getItem('nexora_token');let state={projects:[],policies:[],ops:[]};const $=s=>document.querySelector(s);async function api(url,opt={}){opt.headers={'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{}),...(opt.headers||{})};const r=await fetch(url,opt);const j=await r.json();if(!j.success)throw new Error(j.error?.message||'Request failed');return j.data}
function login(){document.querySelector('#app').innerHTML=`<div class="login card"><h1>Nexora AI</h1><p class="muted">Private development factory</p><input id="u" class="field" value="admin" placeholder="Username"><br><br><input id="p" type="password" class="field" placeholder="Password"><br><br><button class="btn" id="go">Sign in</button><p id="e" class="muted"></p></div>`;$('#go').onclick=async()=>{try{const x=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:$('#u').value,password:$('#p').value})});token=x.token;localStorage.setItem('nexora_token',token);render()}catch(e){$('#e').textContent=e.message}}}
async function render(){try{const [me,health,projects,policies,ops]=await Promise.all([api('/api/me'),api('/api/health'),api('/api/projects'),api('/api/policies'),api('/api/operations')]);state={projects,policies,ops};document.querySelector('#app').innerHTML=`<div class="shell"><aside class="side"><div class="brand">NEXORA <span class="muted">AI</span></div><div class="nav active">⌘ Workspace</div><div class="nav">▣ Projects</div><div class="nav">⌁ Files</div><div class="nav">>_ Terminal</div><div class="nav">✓ Tests & Builds</div><div class="nav">⇄ Git</div><div class="nav">↗ Deployments</div><div class="nav">⚙ Settings</div><div class="nav">🛡 Master Admin</div><div style="margin-top:auto" class="muted">${me.username} · ${me.role}</div></aside><main class="main"><div class="top"><div><h1>Development Workspace</h1><div class="muted">Real operations only · no simulated execution</div></div><button class="btn secondary" id="logout">Log out</button></div><div class="grid"><div class="card"><div class="muted">Platform</div><h2>Operational</h2><span class="status"><span class="dot"></span> API online</span></div><div class="card"><div class="muted">Model server</div><h2>${health.model.available?'Connected':'Not connected'}</h2><div class="muted">${health.model.config.baseUrl}</div></div><div class="card"><div class="muted">Projects</div><h2>${projects.length}</h2><button class="btn" id="newp">Create project</button></div></div><div class="card" style="margin-top:16px"><h2>AI Workspace</h2><textarea id="req" class="field" rows="4" placeholder="Describe a development task for a selected project..."></textarea><br><br><select id="project" class="field">${projects.map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</select><br><br><button class="btn" id="plan">Analyze & plan with connected model</button><pre id="planout" class="terminal">Waiting for a real model request...</pre></div><div class="card" style="margin-top:16px"><h2>Master Admin · Policy Engine</h2><div id="policies">${policies.map(p=>`<label class="row" style="justify-content:space-between;padding:8px 0"><span>${p.key}</span><input type="checkbox" data-key="${p.key}" ${p.enabled?'checked':''}></label>`).join('')}</div></div><div class="card" style="margin-top:16px"><h2>Recent real operations</h2><div class="terminal">${ops.length?ops.map(o=>`[${o.status.toUpperCase()}] ${o.kind} ${o.command||''}\n${o.error||o.output||''}`).join('\n\n'):'No operations yet.'}</div></div></main></div>`;bind()}catch(e){if(/Authentication/.test(e.message)||/Session/.test(e.message)){localStorage.removeItem('nexora_token');token=null;login()}else document.querySelector('#app').textContent=e.message}}
function bind(){$('#logout').onclick=()=>{localStorage.removeItem('nexora_token');token=null;login()};$('#newp').onclick=async()=>{const name=prompt('Project name');const root=prompt('Absolute workspace path','./workspace/'+(name||'project').replace(/\W+/g,'-'));if(name&&root){await api('/api/projects',{method:'POST',body:JSON.stringify({name,root})});render()}};$('#plan').onclick=async()=>{const out=$('#planout');out.textContent='Sending real request...';try{const x=await api('/api/projects/'+$('#project').value+'/plan',{method:'POST',body:JSON.stringify({request:$('#req').value})});out.textContent=JSON.stringify(x,null,2)}catch(e){out.textContent='BLOCKED/ERROR: '+e.message}};document.querySelectorAll('[data-key]').forEach(x=>x.onchange=async()=>{try{await api('/api/policies/'+x.dataset.key,{method:'PATCH',body:JSON.stringify({enabled:x.checked})})}catch(e){x.checked=!x.checked;alert(e.message)}})}
if(token)render();else login();
